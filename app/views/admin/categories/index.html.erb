<div class="w-full">
  <% if notice.present? %>
    <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice"><%= notice %></p>
  <% end %>
  <% content_for :title, "Categories" %>
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Categories</h1>
    <%= link_to "New category", new_admin_category_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium hover:bg-blue-800" %>
  </div>
  <div class="flex mt-4">
    <table class="w-full">
      <thead class="bg-blue-200">
        <tr class="border border-black">
          <th>
          </th>
          <th>
            Name
          </th>
          <th>
            Description
          </th>
          <th>
            Action
          </th>
        </tr>
      </thead>
      <tbody class="bg-blue-50">
        <% @admin_categories.each do |c|%>
          <tr>
            <td class="items-center justify-center px-6 py-4">
              <%= c.image.present? ? image_tag(c.image.variant(:thumb)) : image_tag("https://via.placeholder.com/50")%>
            </td>
            <td class="align-middle text-center">
              <%= c.name%>
            </td>
            <td class="align-middle text-center">
              <%= c.description%>
            </td>
            <td class="flex py-4 justify-center">
              <%= link_to "Show this category", edit_admin_category_path(c), class: "rounded-lg py-3 px-5 bg-blue-400 inline-block font-medium"%>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>